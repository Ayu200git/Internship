<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=<, initial-scale=1.0")
        title #{docTitle}
        link(rel="stylesheet", href="/css/main.css")
        link(rel="stylesheet", href="/css/product.css")
    body 
        header.main-header
          nav.main-header_nav
            ul.main-header_item-list 
                li.main-header_item  
                   a.active(href="/") Shop 
                li.main-header_item  
                   a.active(href="/admin/add-product") add Product
        main
            if prods.length > 0 
                .grid
                    each product in prods 
                        article.card.product-item
                            header.card_header
                                h1.product_title #{product.title}
                            div.card_image
                                img(src="")
                            div.card_content
                                h2.product_price $19.99
                                p.product_description A very interestinh Book.                 
                            .card_action
                                button.btn Add to cart 
            else 
                h1 No products                     
